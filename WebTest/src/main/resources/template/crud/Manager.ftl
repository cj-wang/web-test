package ${packageName}.service;

import cn.walle.framework.common.service.CommonSaveManager;

import ${packageName}.model.${className}Model;

public interface ${className}Manager extends CommonSaveManager<${className}Model> {

}
